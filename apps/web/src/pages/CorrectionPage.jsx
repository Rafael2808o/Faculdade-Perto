import { useState } from 'react';import { useNavigate,useSearchParams } from 'react-router-dom';import { Breadcrumbs } from '../components/Breadcrumbs.jsx';import { Seo } from '../components/Seo.jsx';import { api } from '../services/api.js';
export function CorrectionPage(){const [params]=useSearchParams();const [form,setForm]=useState({entityType:params.get('entityType')||'other',entityId:params.get('entityId')||'',description:'',evidenceUrl:'',name:'',email:''});const [errors,setErrors]=useState({});const [general,setGeneral]=useState('');const navigate=useNavigate();function change(e){setForm({...form,[e.target.name]:e.target.value})}async function submit(e){e.preventDefault();try{await api('/corrections',{method:'POST',body:JSON.stringify({...form,entityId:form.entityId||undefined})});navigate('/obrigado?tipo=correcao')}catch(error){setErrors(error.payload?.error?.fields||{});setGeneral(error.payload?.error?.message||error.message)}}return <><Seo title="Encontrou um erro? | Faculdade Perto" description="Envie uma correção com evidência. Toda alteração passa por moderação antes de mudar um dado público." path="/corrigir"/><header className="page-header"><Breadcrumbs items={[{label:'Encontrou um erro?'}]}/><h1>Ajude o dado a ficar melhor.</h1><p>Sua sugestão entra numa fila de revisão. Nenhuma informação pública é alterada automaticamente.</p></header><section className="section"><form className="form-card" onSubmit={submit}>{general&&<div className="form-error">{general}</div>}<div className="form-grid"><label className="form-field"><span>Tipo do registro</span><select name="entityType" value={form.entityType} onChange={change}><option value="institution">Instituição</option><option value="campus">Campus</option><option value="course">Curso</option><option value="offering">Oferta/registro</option><option value="other">Outro</option></select></label><label className="form-field"><span>URL da evidência</span><input name="evidenceUrl" type="url" value={form.evidenceUrl} onChange={change}/>{errors.evidenceUrl&&<small className="field-error">{errors.evidenceUrl}</small>}</label><label className="form-field full"><span>O que está errado?</span><textarea name="description" value={form.description} onChange={change}/>{errors.description&&<small className="field-error">{errors.description}</small>}</label><label className="form-field"><span>Seu nome (opcional)</span><input name="name" value={form.name} onChange={change}/></label><label className="form-field"><span>Seu e-mail (opcional)</span><input name="email" type="email" value={form.email} onChange={change}/></label></div><button className="primary-button">Enviar para revisão</button></form></section></>}
