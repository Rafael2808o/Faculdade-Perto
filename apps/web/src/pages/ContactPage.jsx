import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { Breadcrumbs } from '../components/Breadcrumbs.jsx';import { Seo } from '../components/Seo.jsx';import { api } from '../services/api.js';
export function ContactPage(){const [form,setForm]=useState({name:'',email:'',subject:'',message:''});const [errors,setErrors]=useState({});const [general,setGeneral]=useState('');const [sending,setSending]=useState(false);const navigate=useNavigate();function change(e){setForm({...form,[e.target.name]:e.target.value});setErrors({...errors,[e.target.name]:undefined})}async function submit(e){e.preventDefault();setSending(true);setGeneral('');try{await api('/contact',{method:'POST',body:JSON.stringify(form)});navigate('/obrigado?tipo=contato')}catch(error){setErrors(error.payload?.error?.fields||{});setGeneral(error.payload?.error?.message||error.message)}finally{setSending(false)}}return <><Seo title="Contato | Faculdade Perto" description="Fale com a equipe do Faculdade Perto. Formulário validado e resposta clara em caso de erro." path="/contato"/><header className="page-header"><Breadcrumbs items={[{label:'Contato'}]}/><h1>Fale com a gente.</h1><p>Dúvida, sugestão ou parceria? Envie sua mensagem. Para corrigir um dado específico, use a página “Encontrou um erro?”.</p></header><section className="section"><form className="form-card" onSubmit={submit} noValidate>{general&&<div className="form-error">{general}</div>}<div className="form-grid"><Field label="Nome" name="name" value={form.name} onChange={change} error={errors.name}/><Field label="E-mail" name="email" type="email" value={form.email} onChange={change} error={errors.email}/><Field label="Assunto" name="subject" value={form.subject} onChange={change} error={errors.subject}/><label className="form-field full"><span>Mensagem</span><textarea name="message" value={form.message} onChange={change}/>{errors.message&&<small className="field-error">{errors.message}</small>}</label></div><button className="primary-button" disabled={sending}>{sending?'Enviando…':'Enviar mensagem'}</button></form></section></>}
function Field({label,error,...props}){return <label className="form-field"><span>{label}</span><input {...props}/>{error&&<small className="field-error">{error}</small>}</label>}
